PORT=3000
SESSION_SECRET=super_secret_cafeteria_token_key_12345

# ── Option 1: Supabase / PostgreSQL (recommended for Vercel) ──────────────
# Get this from: Supabase Dashboard → Settings → Database → URI
DATABASE_URL=postgresql://postgres:[PASSWORD]@db.xxxx.supabase.co:5432/postgres

# ── Option 2: Local MySQL ─────────────────────────────────────────────────
# Leave DATABASE_URL empty and fill these in instead
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=smart_cafeteria
